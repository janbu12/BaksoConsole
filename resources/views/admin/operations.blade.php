<x-layouts.app title="Admin"><h1 class="text-3xl font-black">Bakso Insight</h1><div class="mt-5 grid gap-4 md:grid-cols-4">@foreach($stats as $label=>$value)<div class="rounded-2xl bg-slate-900 p-5"><p class="text-slate-400">{{ $label }}</p><b class="text-2xl">{{ is_numeric($value)?number_format($value):$value }}</b></div>@endforeach</div><div class="mt-8 grid gap-6 lg:grid-cols-3"><form method="POST" action="/admin/units" class="rounded-2xl bg-slate-900 p-5 space-y-2">@csrf<h2 class="font-bold">Tambah Unit</h2><input name="name" placeholder="Nama" class="w-full rounded bg-slate-950 p-2"><input name="code" placeholder="Kode" class="w-full rounded bg-slate-950 p-2"><input name="daily_price" placeholder="Harga/hari" class="w-full rounded bg-slate-950 p-2"><input name="max_players" placeholder="Pemain" class="w-full rounded bg-slate-950 p-2"><button class="rounded bg-orange-500 p-2">Simpan</button></form><form method="POST" action="/admin/categories" class="rounded-2xl bg-slate-900 p-5 space-y-2">@csrf<h2 class="font-bold">Kategori</h2><input name="name" class="w-full rounded bg-slate-950 p-2"><button class="rounded bg-orange-500 p-2">Simpan</button></form><form method="POST" action="/admin/combos" class="rounded-2xl bg-slate-900 p-5 space-y-2">@csrf<h2 class="font-bold">Bakso Combo</h2>@foreach(['name','duration_days','controller_count','price'] as $f)<input name="{{ $f }}" placeholder="{{ $f }}" class="w-full rounded bg-slate-950 p-2">@endforeach<button class="rounded bg-orange-500 p-2">Simpan</button></form></div><h2 class="mt-8 text-xl font-bold">Booking terbaru</h2><div class="mt-3 space-y-2">@foreach($bookings as $b)<div class="flex justify-between rounded-xl bg-slate-900 p-4"><span>{{ $b->user->name }} · {{ $b->unit->name }} · {{ $b->status->value }}</span><div class="flex gap-2"><form method="POST" action="/admin/bookings/{{ $b->id }}/confirm">@csrf<button>Konfirmasi</button></form><form method="POST" action="/admin/bookings/{{ $b->id }}/start">@csrf<button class="text-orange-400">Mulai Rental</button></form></div></div>@endforeach</div></x-layouts.app>
