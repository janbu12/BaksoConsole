<x-layouts.app title="Profil"><h1 class="text-3xl font-black">Profil</h1><form method="POST" action="/profile" class="mt-6 max-w-xl space-y-4">@csrf @method('PUT')<input name="name" value="{{ $user->name }}" class="w-full rounded-xl bg-slate-900 p-3"><input name="phone" value="{{ $user->profile?->phone }}" placeholder="Telepon" class="w-full rounded-xl bg-slate-900 p-3"><textarea name="address" placeholder="Alamat" class="w-full rounded-xl bg-slate-900 p-3">{{ $user->profile?->address }}</textarea><input type="date" name="date_of_birth" value="{{ $user->profile?->date_of_birth?->format('Y-m-d') }}" class="w-full rounded-xl bg-slate-900 p-3"><button class="rounded-xl bg-orange-500 px-5 py-3 font-bold">Simpan</button></form></x-layouts.app>
